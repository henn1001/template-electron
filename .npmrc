# Allow git dependencies (required for @electron/node-gyp via @electron/rebuild).
# npm 12 defaults allow-git=none and refuses git+https deps with EALLOWGIT.
# Cannot be set via package.json by design — must be .npmrc / CLI / env.
allow-git=all
